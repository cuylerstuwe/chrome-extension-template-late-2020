export default async function onBackgroundReady() {
    // TODO: Write whatever code should execute once the background script is ready.
    //  This function should run once on init, after the extension's core message pump and background modules are prepared to send/receive.
}