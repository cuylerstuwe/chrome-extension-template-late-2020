const urlMatchingPageConfigurations = ([
    {
        urlMatcher: /.*/,
        modules: [

        ],
        initialState: ({

        }),
        main: async () => {

        }
    }
]);

export default urlMatchingPageConfigurations;
